Netzwerkkonfiguration

CA Zertificate

/etc/ssl/certs


Netzwerkeinstelunge mit Kommando nmcli verwalten

wie hostname und feste IP Adresse definieren?

DHCP ohne Router in virtuelle umgebung

Netzmaske
Gateway

Wireless
iwconfig, iwlist

ifconfig und route veraltet man soll Kommando "ip" verwenden


BAsis LAN-Konfiguration

/etc/hosts

DNS Konfiguration

/etc/resolv.conf

Programme: dnsmasq, dhcpd, bind
