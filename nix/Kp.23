Partionzitonswerkzeuge:

- parted
- (fdisk)
- gparted (parted mit GUI)


Befehl 
df  -h	-t


mount
unmount

Datei /etc/fstab stuert, welche Dateisysteme beim Rechnerstart automatisch in den Verzeichnisbaum integriert werden und welchen Optionen.

btrfs
snapshot

LVM
Kernelmodul dm_mod
Befehle um LVM zu verwalten:



SMART
smartctl -A 
smartctl --attributes

