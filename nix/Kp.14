Netzwerk

hostname -I
ip link     Liste aller Netzwerkschnittschtellen

(
lo: localhost
eth: Ethernet
wlan:
)


ping
ping -c
ip route

traceroute




=============
SSH
=============
Extra Datei anlgen

scp
ssh -X

/etc/ssh/ssh_config


ssh dateisystem
sshfs


======
FTP, SFTP, rsync, wget, curl, lftp
=======
