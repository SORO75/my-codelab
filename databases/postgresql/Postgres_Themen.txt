==================================
    Datenbank (RDBMS) Lernpfad
==================================

Datenbanken:
(RDBMS) Oracle, Postgres, MySQL, SQLite, Derby MSSQL
(NOSQL) Mongo, CouchDB, Redis, HBase, Riak, Neo4J, Snowflake




---------------------------------
    +Installation
---------------------------------
    - Windows
    - Linux
    - MacOS
    - Installationsordner
    - Systempfad
    - Datenordner
    - Clusteraufbau
    - Using in the Cloud
    - Anbindung an der Cloud Instanzen
    - Docker Instanz
    - Kubernetes
    - Connection (SSL)

    
---------------------------------
    + DBA
---------------------------------    
    - root user
    - Listner
    - Port
    - Prozesstree
    - Start, Stopp, DB_Information
    - Konfiguration
    - Monitoring
    - Clustersteuerung
    - Shared Memory
    - User CRUD
    - Rollen CRUD
    - Berechtigung, Rollen
    - Replication
    - DB Dump
    - Logging
    - Sessionanalyse
    - Performance optiemierung
    - Kopie
    - Migration
    - Multi-version Concurrency Control
    - Backup
    - Admintools
    - Updating, Upgrating
    
+DML
    Select
    Insert
    Update
    Delete
+DDL
    Create
    Alter
    Drop
    Truncate
+DCL
    - Grant
    - Revoke
+TCL
    -savepoint
    - rollback
    - commit
+Datenbankdesign
    ACID
    1,2,3 N-Norm
    Snowflakeschema

+ SQL Programmierung 
    -SQL Version
    - Prozedurale SQL
    - Pakete

----------------------------
+Datenbankfunktionalität
----------------------------
    - DD
    - Tablespace
    - Schema
    - Views
    - Indizis
    - Prozeduren
    - Funktionen
    - Trigger
    - Queues
    - Queuetabellen
    - Datentypen
    - Typen
    - Sequencen
    - Materialized Views
    - Datenbanklinks
    - XML
    - JSON
    - REST API 
    - Scheduler
    - DBMS Jobs
    - Export, Import
    - File observer
    - Sprach APIs
    - OLAP
    - Berichte
    - Stream
    - Transaktionsteuerung (optimistisch, pessimistisch)
    - Table Partionierung
    - Operatoren
    - CTID
    - Parallel Query
    - Columnstory DB
    - Binary Daty
    - BLOB
    - Thread
    - Free space mapping (FSM)





#####-----------------------------------
    +SQL-----------------------------------
    - SQL Dialekte
    - ANSI SQL
    - SQL Standards
    - SQL Syntax


-----------------------------------    +SQL Statements
-----------------------------------
    create a new database
    create a new table
    insert data into a table
    insert multiple rows into a table
    insert data from another table
    insert data with a subquery

    update data in a table
    delete data from a table
    select data from a table
    select specific columns from a table
    select distinct values from a table
    select with conditions (WHERE)
    select with multiple conditions (AND, OR)
    select with pattern matching (LIKE)
    select with range conditions (BETWEEN)
    select with NULL values (IS NULL, IS NOT NULL)
    select with ordering (ORDER BY)
    select with grouping (GROUP BY)
    select with aggregation functions (COUNT, SUM, AVG, MAX, MIN)
    select with having clause (HAVING)
    select with joins (INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL OUTER JOIN)
    select with subqueries (nested queries)
    select with set operations (UNION, INTERSECT, EXCEPT)
    select with window functions (ROW_NUMBER, RANK, DENSE_RANK)
    select with common table expressions (CTE)
    select with recursive queries
    select with pivoting (if supported)
    select with lateral joins (if supported)
    select with JSON functions (if supported)
    select with XML functions (if supported)
    select with full-text search (if supported)
    select with spatial queries (if supported)
    select with temporal queries (if supported)
    select with materialized views (if supported)
    select with partitioned tables (if supported)
    select with foreign data wrappers (if supported)    
    join tables


eine DHW erstellen





    




